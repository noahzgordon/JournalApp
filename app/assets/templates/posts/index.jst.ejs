<ul>
  <% _.each(posts, function (post) { %>
    <li><%= post.get("title") %>
        <button class="post-delete" data-id="<%= post.get("id") %>">
          Delete
        </button>
    </li>
  <% }); %>
</ul>